# Ignore unnecessary files for Docker build
node_modules
.git
.gitignore
README.md
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Keep only what we need for the container
!dist/
!package*.json
